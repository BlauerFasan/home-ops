---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: unifi-tls
spec:
  secretName: unifi-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "unifi.${SECRET_DOMAIN_2}"
  dnsNames: ["unifi.${SECRET_DOMAIN_2}"]
